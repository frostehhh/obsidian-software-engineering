A globally distributed network of proxy servers that serve content to users generally static content. 
# Benefits
Quicker retrieval times for users
Less load on server where the content originated from

# Types of CDNs
## Push CDN
Server content is consistently pushed into CDN.

Better for sites with small traffic and content that isn't updated frequently.

## Pull CDN
When users request data, the CDN pulls content from the corresponding server