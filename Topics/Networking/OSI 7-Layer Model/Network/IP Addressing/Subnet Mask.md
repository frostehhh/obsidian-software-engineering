- allows a HOST to determine if an IP address it needs to communicate is local or remote, which influences if it needs to use a gateway or can communicate locally
- Configured on a host device to to an IP address e.g. 255.255.0.0 & this is the same as a /16 prefix
- Used to identify which part of an IP address is the network part and the host part
![[OSI-LAYER3-NETWORK-4.png]]