depends-on configuration is used so that a bean can depend on another bean. That means usually that one bean is set as a property of another.[^1]


[^1]: https://docs.spring.io/spring-framework/reference/core/beans/dependencies/factory-dependson.html