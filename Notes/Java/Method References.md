---
tags:
  - notes
  - programming-languages/java
Draft: true
---

`::` operator

# Examples
- String array to stream to filter -> collect as list -> cast to array via `.toArray(String[]::new)`.
	- String[] refers to the array type
	- `new` refers to the constructor method
	- 